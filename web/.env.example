# Web layer (Next.js) uses @vercel/postgres, which reads POSTGRES_URL.
# On Vercel this is injected automatically when you link a Postgres/Supabase DB.
# For local dev, set it to your Supabase connection string (use the *pooled* URL).
POSTGRES_URL=postgres://user:pass@host:6543/postgres
